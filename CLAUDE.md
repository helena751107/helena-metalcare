# CLAUDE.md — 헬레나 OS 헌법

> 이 문서는 AI 에이전트(Claude Code)가 이 레포지토리에서
> 자율적으로 작업할 때 따라야 할 규칙과 패턴을 정의한다.

## 레포 정체성
- **목적:** 누나의 조현병을 "죽기 전에 진짜로 알아가는" 과정을 기록. MetalCare = MentalCare 오타를 재해석("금속처럼 강력한 하나의 기준").
- **경계 (필수):**
  - 누나 신원(이름·위치·사진) 절대 노출 금지. Boss가 부른 "REDACTED"는 실명 추정 — 공개문에 쓰지 말 것.
  - 의료 단정 표현 금지 ("전문의 확인 필요"로).
  - 환청·약·증상은 "이해를 위한 기록", 선정적 전시 금지.

## 작업 원칙
- **커밋 자주, 작게**: 기능 단위로 쪼개서 커밋
- **설명 남겨라**: "왜"를 커밋 메시지에 포함
- **깨져도 괜찮다**: 일단 작동, 나중에 개선

## 파일 구조
- `index.html` = 랜딩 페이지 (루트)
- `docs/` = 기획/설계
- `data/` = JSON 저장소
- `scripts/` = 자동화 스크립트
- `assets/` = 이미지/CSS/JS

## AI 행동 규칙
- 작업 전 `git pull`로 최신 상태 확인
- 완료 후 `git push` 자동 실행
- 커밋 메시지에 작업 맥락 포함
